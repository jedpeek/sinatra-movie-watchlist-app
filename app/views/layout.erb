<!DOCTYPE html>
<html>
  <head>
    <title>Watchlist</title>
    <!-- Bootstrap -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  </head>

  <body>
    <div class='container-fluid' align='center'>
      <nav class="navbar navbar-default">
        <div class="container-fluid">
          <!-- Brand and toggle get grouped for better mobile display -->
          <div class="navbar-header">
            <% if logged_in? %>
            <a class="navbar-brand" href="/movies">Watchlist</a>
            <% else %>
            <a class="navbar-brand" href="/">Watchlist</a>
            <% end %>
          </div>
          <div class="nav navbar-nav navbar-left">
            <li><a href="/movies">Movies </a></li>
          </div>
            <div class="nav navbar-nav navbar-right"><!-- add login/logout functionality -->
              <% if !logged_in? %>
              <li><a href="/login">Login</a></li>
              <% else %>
              <li><a href="/logout">Logout</a></li>
              <% end %>
            </div>
          </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
      </nav>

      <br>
        <h1 align="center">Welcome to Watchlist <% if logged_in? %> <%= current_user.name.capitalize %> <%end%></h1>
      <br>
      <div class="container">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
